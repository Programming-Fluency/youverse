export default function HomePage() {
  return (
    <div className="p-6">
      <p className="text-sm text-muted-foreground">
        Temporary placeholder — the real home feed (AGENTS.md section 10) isn&apos;t built yet.
        This page exists only so the navbar can be tested.
      </p>
    </div>
  );
}
